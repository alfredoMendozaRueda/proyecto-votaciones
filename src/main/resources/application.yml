spring:
  application:
    name: proyecto-votaciones

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/bbdd_amr_elecciones?useUnicode=true&characterEncoding=UTF-8}
    username: ${DB_USUARIO:root}
    password: ${DB_CONTRASENA:}

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: false

  thymeleaf:
    cache: false

server:
  port: ${SERVER_PORT:8080}
